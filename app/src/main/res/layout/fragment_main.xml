<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.licor.gourmet.MainFragment">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            android:id="@+id/photoButton"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:layout_marginBottom="60dp"
            android:background="@drawable/photo_button_img"
            android:onClick="takePhoto"
            android:layout_above="@+id/albumButton"
            android:layout_centerHorizontal="true" />

        <Button
            android:id="@+id/albumButton"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_alignParentBottom="true"
            android:layout_marginBottom="140dp"
            android:layout_marginStart="20dp"
            android:layout_toEndOf="@+id/photoButton"
            android:background="@drawable/album_button_img"
            android:onClick="selectPhoto" />
    </RelativeLayout>

</FrameLayout>
